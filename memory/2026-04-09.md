# 2026-04-09

- User preference: keep replies concise and do not reiterate the whole conversation in normal replies. Summarize only the necessary delta when helpful, and provide more detail only if asked.
- Canon preference: when building recurring processes or operational flows, build them as concrete tools or automations, not just conversational promises.
- Canon distinction: a tool is an on-demand capability with explicit input/output; an automation is that capability wired to run from triggers, schedules, or state changes.
- Archive convention: use `/home/sai/.openclaw_archive` for items intentionally archived outside the active workspace.
- Legacy store rule: treat `/home/sai/.openclaw.old` as import-only and do not use it as part of the current active OpenClaw space unless the user explicitly asks to import something from it.
- Archive practice: when retiring or isolating old sensitive material, move it into `/home/sai/.openclaw_archive` rather than leaving it in active legacy paths.
- User assessment: the prior "second brain" memory database setup was not working out well and should be archived rather than treated as an active system.
- Migration preference: preserve/import Google model support from legacy config, but archive the rest of the old `openclaw.json` and legacy authentication/config material unless specifically requested.
- User preference: provide healthy debate rather than reflexive agreement. If agreeing, do so only with high confidence; otherwise state the uncertainty and why confidence is not high.
- Architecture decision: do not promote transitional software-development rules into core capabilities when DevClaw is expected to become the defining software-development control plane.
